---
interface Props {
  text: string;
}
const { text } = Astro.props;
---

<p class="eyebrow label-mono">{text}</p>

<style>
  .eyebrow {
    color: var(--navy);
    margin-bottom: var(--space-sm);
  }
</style>
