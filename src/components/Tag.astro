---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span class="tag label-mono">{label}</span>

<style>
  .tag {
    display: inline-block;
    color: var(--navy);
    border: 1px solid var(--navy);
    border-radius: 0;
    padding: 3px 8px;
  }
</style>
